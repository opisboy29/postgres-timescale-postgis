# Replication-specific overrides
# Copy to .env.replica (not tracked) and adjust as needed.

# Credentials/slot used by replica tooling
POSTGRES_REPLICATION_USER=replicator
POSTGRES_REPLICATION_PASSWORD=replicator_password
POSTGRES_REPLICATION_SLOT=replica_slot_1
POSTGRES_REPLICA_APPLICATION_NAME=replica_1

# How the replica reaches the primary container
PRIMARY_HOST=db
PRIMARY_PORT=5432

# Replica container settings
DB_REPLICA_DATA_PATH=./data_timescale_replica
# Set to 0 to let Docker choose a free host port automatically
DB_REPLICA_PORT=5440

# Optional: force docker compose command/service names
# COMPOSE_CMD="docker compose"
# PRIMARY_SERVICE=db
